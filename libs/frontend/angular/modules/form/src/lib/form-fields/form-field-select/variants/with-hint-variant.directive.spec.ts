import { WithHintVariantDirective } from './with-hint-variant.directive';

describe('OptionTemplateWithHintDirective', () => {
  it('should create an instance', () => {
    const directive = new WithHintVariantDirective();
    expect(directive).toBeTruthy();
  });
});
