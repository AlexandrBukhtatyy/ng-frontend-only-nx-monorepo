import { OptionTemplateWithHintDirective } from './option-template-with-hint.directive';

describe('OptionTemplateWithHintDirective', () => {
  it('should create an instance', () => {
    const directive = new OptionTemplateWithHintDirective();
    expect(directive).toBeTruthy();
  });
});
